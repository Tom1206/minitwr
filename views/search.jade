doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='description', content='Mini Twitter Projet ProgWeb L1 S2 2015')
    link(rel='shortcut icon', href='assets/ico/favicon.png')
    title Twitchers - Search
    script(src='assets/js/jquery.min.js')
    // Bootstrap core CSS
    link(href='assets/css/bootstrap.css', rel='stylesheet')
    // Custom styles
    link(href='assets/css/style.css', rel='stylesheet')
  body
    // navbar
    #navigation.navbar.navbar-default.navbar-fixed-top
      .container
        .navbar-header
          button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='home')
            span.glyphicon.glyphicon-leaf
          a.navbar-brand(href='public/#{user._id}')
            | #{user.username}
        .navbar-collapse.collapse
          .col-sm-9.col-md-7.pull-left
            form.navbar-form(role='search', action='/search', method='post', enctype='plain')
              .input-group
                input.form-control(type='text', placeholder='Search (profile, tweet, location, etc...)', name='research')
                .input-group-btn
                  button.btn.btn-default(type='submit')
                    span.glyphicon.glyphicon-search
          .col-sm-3.col-md-3.pull-left
            ul.nav.navbar-nav
              li
                a(title='Home', href='/home')
                  span.glyphicon.glyphicon-home
              li
                a(title='Messages', href='/message')
                  span.glyphicon.glyphicon-envelope
              li
                a(title=' modify profile', href='/profile')
                  span.glyphicon.glyphicon-cog
              li
                a(title='LogOut', href='/signout')
                  span.glyphicon.glyphicon-off
    br
    br
    br
    br
.container
  .row
    .col-md-12
      h2.center  Users
      hr
      .center
        script.
          var j=0;
          var users= !{JSON.stringify(users)};
          while(users[j].username != null)
          {
            document.write('<a href="public/' + users[j]._id + '">' + users[j].username + "</a>");
            document.write('<br/>');
            j++;
          }
        br
        br
  .container
    .row
      .col-md-12
        h2.center  Location
        hr
        script.
          var j=0;
          var tweets= !{JSON.stringify(loc)};
          var user= !{JSON.stringify(user)};
          while (tweets[j].nickname != null)
          {
            document.write('<div class="row">');
            document.write('<div class="col-md-6 col-md-offset-3 boxtweets">');
            document.write('<div class="tweethead">');
            if (tweets[j].id == user._id)
            {
              document.write('<form role="form" action="/deletetweet" method="post" enctype="plain">');
              document.write('<div class="form-group">');
              document.write('<input type="hidden" name="idtweet" class="form-control" value="' + tweets[j]._id + '">');
              document.write('<button type="submit" class="glyphicon glyphicon-remove-circle" style="float:right; color:red; border: none; background: none; margin: 0px;"></button>');
              document.write('</div>');
              document.write('</form>');
            }
            document.write('<a href="public/' + tweets[j].id + '"> <p class="nickname"><strong>&nbsp' + tweets[j].nickname + '</strong></p></div></a>');
            document.write('<div class="bodytweet">'+tweets[j].tweet+'</div>');
            document.write('<div id="footertweet"><p>'+tweets[j].date+tweets[j].location+'</p>');

            document.write('</div>');
            document.write('</div>');
            document.write('</div>');
            document.write('<br/>');
            j++;
          }

        br
        br
  .container
    .col-md-12
        h2.center Tweets
        hr
      script.
        var i=0;
        var tweets= !{JSON.stringify(tweets)};
        var user= !{JSON.stringify(user)};
        while (tweets[i].nickname != null)
        {
          document.write('<div class="row">');
          document.write('<div class="col-md-6 col-md-offset-3 boxtweets">');
          document.write('<div class="tweethead">');
          if (tweets[i].id == user._id)
          {
            document.write('<form role="form" action="/deletetweet" method="post" enctype="plain">');
            document.write('<div class="form-group">');
            document.write('<input type="hidden" name="idtweet" class="form-control" value="' + tweets[i]._id + '">');
            document.write('<button type="submit" class="glyphicon glyphicon-remove-circle" style="float:right; color:red; border: none; background: none; margin: 0px;"></button>');
            document.write('</div>');
            document.write('</form>');
          }
          document.write('<a href="public/' + tweets[i].id + '"> <p class="nickname"><strong>&nbsp' + tweets[i].nickname + '</strong></p></div></a>');
          document.write('<div class="bodytweet">'+tweets[i].tweet+'</div>');
          document.write('<div id="footertweet"><p>'+tweets[i].date+tweets[i].location+'</p>');

          document.write('</div>');
          document.write('</div>');
          document.write('</div>');
          document.write('<br/>');
          i++;
        }

script(src='../assets/js/bootstrap.js')
